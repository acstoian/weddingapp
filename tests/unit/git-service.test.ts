import { describe, it } from "vitest";

describe("GitService", () => {
  it.todo("creates branch for new invitation");
  it.todo("writes invitation-config.json to branch");
  it.todo("updates existing file with correct sha");
  it.todo("handles 404 when file does not exist yet");
});
