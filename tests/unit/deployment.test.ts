import { describe, it } from "vitest";

describe("DeploymentService", () => {
  it.todo("pollStatus returns READY");
  it.todo("pollStatus returns ERROR on failed deployment");
  it.todo("createOrUpdateProject returns projectId and url");
  it.todo("triggerDeploy returns deploymentId");
  it.todo("deleteProject resolves without error");
});
