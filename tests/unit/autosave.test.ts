import { describe, it } from "vitest";

describe("useAutosave", () => {
  it.todo("fires after 2s debounce");
  it.todo("does not fire before 2s debounce elapses");
  it.todo("cancels pending save when fields change again");
  it.todo("calls PATCH /api/invitations/:id with updated fields");
});
