---
phase: 01-free-tier-mvp
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/gallery/page.tsx
  - src/app/(dashboard)/pricing/page.tsx
  - src/app/api/invitations/route.ts
  - src/components/dashboard/InvitationCard.tsx
  - src/components/dashboard/EmptyState.tsx
  - src/components/gallery/TemplateGallery.tsx
  - src/components/gallery/TemplateCard.tsx
  - src/components/gallery/TemplatePreviewModal.tsx
  - src/components/gallery/FreeLimitBanner.tsx
  - tests/e2e/gallery.spec.ts
autonomous: false
requirements:
  - REQ-01
  - REQ-04
must_haves:
  truths:
    - "Dashboard shows invitation cards (or empty state) for the authenticated user"
    - "Clicking '+ New' opens the gallery page (not a modal) from the dashboard"
    - "Gallery shows all 6 template cards with thumbnail, name, and event type tag"
    - "Filter tabs All/Wedding/Baptism correctly filter to 6/4/2 templates"
    - "Clicking a template card opens a full-screen preview modal with desktop/mobile toggle"
    - "'Use this template' in the modal creates an invitation and navigates to the editor"
    - "Free tier limit banner appears at top of gallery when user has 3+ drafts"
    - "Dashboard cards show title, status badge (Draft/Publishing/Live), and last-edited date"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard page with invitation card grid and empty state"
    - path: "src/app/(dashboard)/gallery/page.tsx"
      provides: "Template gallery with filter tabs and FreeLimitBanner"
    - path: "src/components/gallery/TemplatePreviewModal.tsx"
      provides: "Full-screen preview modal with desktop/mobile toggle and 'Use this template' CTA"
    - path: "src/app/api/invitations/route.ts"
      provides: "GET /api/invitations (list) + POST /api/invitations (create) with FeatureGate check"
  key_links:
    - from: "src/app/(dashboard)/gallery/page.tsx"
      to: "src/app/api/invitations"
      via: "POST on 'Use this template' click to create invitation"
    - from: "src/components/gallery/TemplateGallery.tsx"
      to: "src/lib/templates/registry.ts"
      via: "import { templates } to render template cards"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/app/api/invitations"
      via: "GET request to load user's invitation list"
user_setup:
  - service: neon
    why: "Database must exist and DATABASE_URL set for invitation list API to work"
    env_vars:
      - name: DATABASE_URL
        source: "Neon dashboard -> Project -> Connection string (pooled)"
---

<objective>
Build the dashboard (invitation card grid + empty state) and template gallery (filter tabs + full-screen preview modal + "Use this template" CTA) with the invitations CRUD API backing them.

Purpose: This is the primary product surface. Users discover templates here and start the invitation creation flow. The gallery's quality directly determines conversion from signup to editor engagement.
Output: Navigable dashboard + gallery pages, invitation list/create API, and a human checkpoint to verify the visual quality meets production standards.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-free-tier-mvp/01-CONTEXT.md
@.planning/phases/01-free-tier-mvp/01-RESEARCH.md
@.planning/phases/01-free-tier-mvp/01-01-SUMMARY.md
@.planning/phases/01-free-tier-mvp/01-03-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md Gallery Filter code example -->
```typescript
// components/gallery/TemplateGallery.tsx
type FilterTab = 'ALL' | 'WEDDING' | 'BAPTISM'

const [activeFilter, setActiveFilter] = useState<FilterTab>('ALL')
const filtered = templates.filter(t =>
  activeFilter === 'ALL' ? true : t.category === activeFilter
)
```

<!-- Invitation list API shape -->
```typescript
// GET /api/invitations response
{ invitations: Array<{
  id: string; templateId: string; title: string;
  status: 'DRAFT' | 'PUBLISHING' | 'LIVE' | 'FAILED';
  liveUrl: string | null; updatedAt: string;
}> }

// POST /api/invitations request body
{ templateId: string }

// POST response
{ invitation: { id: string; status: 'DRAFT' } }
// On limit exceeded: 403 { error: 'Free tier: 3 drafts max' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invitations CRUD API + Dashboard + Gallery UI</name>
  <files>
    src/app/api/invitations/route.ts,
    src/app/(dashboard)/dashboard/page.tsx,
    src/app/(dashboard)/gallery/page.tsx,
    src/app/(dashboard)/pricing/page.tsx,
    src/components/dashboard/InvitationCard.tsx,
    src/components/dashboard/EmptyState.tsx,
    src/components/gallery/TemplateGallery.tsx,
    src/components/gallery/TemplateCard.tsx,
    src/components/gallery/FreeLimitBanner.tsx
  </files>
  <action>
    **UI MANDATORY:** Before writing any component, run:
    ```
    python3 .claude/skills/ui-ux-pro-max/scripts/search.py "wedding saas dashboard card grid minimal" --design-system -p "Save the Date" --page "dashboard"
    python3 .claude/skills/ui-ux-pro-max/scripts/search.py "loading skeleton card grid" --domain ux -n 5
    ```
    Apply the design system recommendations to all dashboard and gallery components.

    **1. `src/app/api/invitations/route.ts`** — invitation CRUD:
    - `GET` handler:
      - Authenticate via `auth()` from `@clerk/nextjs/server`; return 401 if not signed in
      - Query `db.select().from(invitations).where(eq(invitations.userId, userId)).orderBy(desc(invitations.updatedAt))`
      - Return `{ invitations: [...] }` with fields: id, templateId, title, status, liveUrl, updatedAt
    - `POST` handler:
      - Authenticate user
      - Call `featureGate.canCreateDraft(userId)` — return 403 `{ error: reason }` if not allowed
      - Validate request body: `{ templateId: string }` — 400 if missing or not in template registry
      - Insert to DB: `db.insert(invitations).values({ userId, templateId, title: '', fields: {}, status: 'DRAFT' }).returning()`
      - Return 201 `{ invitation: { id, status: 'DRAFT' } }`

    **2. `src/app/(dashboard)/dashboard/page.tsx`** — Server Component:
    - Authenticate, fetch invitation list from DB directly (server component, not fetch)
    - If 0 invitations: render `<EmptyState />`
    - Else: render card grid (2 cols mobile, 3 cols lg) with `<InvitationCard />` for each + `<InvitationCard type="new" />` as the "+ New" card
    - Import `<TopNav />` is already in the layout; no nav needed here

    **3. `src/components/dashboard/EmptyState.tsx`** — per CONTEXT.md:
    - Welcoming banner: "Creeaza invitatii digitale frumoase pentru momentele tale speciale" (RO default)
    - Prominent CTA button linking to `/gallery`
    - Use lucide-react icon (e.g., `Sparkles` or `Heart`) — NOT emoji

    **4. `src/components/dashboard/InvitationCard.tsx`** — per CONTEXT.md:
    - Props: `{ invitation: InvitationRow }` or `{ type: 'new' }`
    - For invitation card: template thumbnail (`<Image>`), title (user-set or "Fara titlu" if empty), status badge (Draft=gray, Publishing=amber, Live=green), last-edited date formatted with date-fns `ro` locale
    - Actions on hover: Edit button (links to `/editor/{id}`), Copy URL (visible only if Live), Delete button (shows confirmation dialog — use shadcn `AlertDialog`)
    - For "+ New" card: dashed border, plus icon, "Invitatie noua" text, links to `/gallery`
    - All cards: `cursor-pointer`, hover state (subtle shadow lift), `transition-shadow duration-200`

    **5. `src/app/(dashboard)/gallery/page.tsx`** — Server Component wrapper + Client Component:
    - Check draft count server-side; pass `{ atLimit: boolean }` to `<TemplateGallery />`
    - Render `<TemplateGallery atLimit={atLimit} />`

    **6. `src/components/gallery/TemplateGallery.tsx`** — Client Component:
    - `FreeLimitBanner` at top (only if `atLimit`)
    - Filter tab row: "Toate (6)" | "Nunta (4)" | "Botez (2)" — active tab has solid style, inactive is ghost
    - Card grid: 2 cols mobile, 3 cols lg — uses `<TemplateCard />` for each filtered template

    **7. `src/components/gallery/TemplateCard.tsx`**:
    - Thumbnail `<Image>` (aspect-ratio: 2/3 — portrait card)
    - Template name + category tag (badge: "Nunta" or "Botez")
    - Click handler: opens `<TemplatePreviewModal templateId={id} />`
    - `cursor-pointer`, hover: scale thumbnail 1.02 (use `transition-transform duration-200`)

    **8. `src/components/gallery/FreeLimitBanner.tsx`**:
    - Amber/warning colored banner at top of gallery
    - Text: "Ai atins limita de 3 ciorne. Publica sau sterge o invitatie existenta pentru a crea una noua."
    - Icon: `AlertTriangle` from lucide-react

    **9. `src/app/(dashboard)/pricing/page.tsx`** — Static pricing page per CONTEXT.md:
    - 3 tier cards: Free / Gold / Platinum
    - Free: 3 drafts, 1 published, basic templates — current plan badge
    - Gold: unlimited invitations, PDF export, priority support — "Coming soon" CTA (not a Stripe link yet)
    - Platinum: all Gold features + WhatsApp bulk send — "Coming soon" CTA
    - Phase 2 will wire the actual Stripe Checkout buttons; for now the buttons are disabled with tooltip "Coming soon"
    - Use lucide-react `Check` icons for feature lists, NOT emoji checkmarks
  </action>
  <verify>
    <automated>pnpm build && pnpm test:unit</automated>
  </verify>
  <done>
    `pnpm build` exits 0. Dashboard renders invitation cards or empty state. Gallery renders all 6 template cards with working filter tabs. Pricing page shows 3 tier cards. No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Template preview modal with desktop/mobile toggle</name>
  <files>
    src/components/gallery/TemplatePreviewModal.tsx
  </files>
  <action>
    **UI MANDATORY:** Run before implementing:
    ```
    python3 .claude/skills/ui-ux-pro-max/scripts/search.py "full screen modal preview desktop mobile toggle" --domain ux -n 5
    ```

    `TemplatePreviewModal.tsx` — Client Component:
    - Props: `{ templateId: string; open: boolean; onClose: () => void }`
    - Use shadcn `Dialog` with `DialogContent` at full-screen size (`w-screen h-screen max-w-none rounded-none`)
    - Header: template name (left), close button (right), desktop/mobile toggle (center)
    - Toggle: `<Monitor>` icon = desktop, `<Smartphone>` icon = mobile (lucide-react). Active state has filled/solid look.
    - Preview area:
      - Desktop mode: renders template component at full width inside a scrollable container
      - Mobile mode: renders template in a centered frame with `w-[375px]` and device-like border (rounded corners, subtle shadow to simulate phone)
    - Template render: use `dynamic(() => import(`@/components/templates/${component}`), { ssr: false })` to avoid SSR hydration mismatch. Pass sample data fixture when no invitation yet selected.
    - Footer bar: "[← Back to gallery]" (ghost button) + ["Use this template →"] (primary button, full width on mobile)
    - "Use this template" handler:
      1. `POST /api/invitations` with `{ templateId }`
      2. On 201: `router.push('/editor/' + invitation.id)`
      3. On 403 (limit): close modal, show inline error (do NOT open gallery with banner — the banner is already visible if atLimit)
      4. Show loading state on button during POST (`disabled` + spinner)

    **Important:** Template components rendered in the modal use `dynamic` with `ssr: false`. Pass a sample `InvitationFields` fixture for the preview:
    ```typescript
    const PREVIEW_FIXTURE: InvitationFields = {
      title: 'Elena & Andrei',
      names: 'Elena & Andrei',
      eventDatetime: '2026-08-15T16:00:00Z',
      venueName: 'Catedrala Sfantul Iosif',
      venueAddress: 'Calea Victoriei 56, Bucuresti',
      personalMessage: 'Va asteptam cu drag la nunta noastra.',
      dresscodeRsvpNote: 'Smart casual',
    }
    ```
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    `pnpm build` exits 0. TemplatePreviewModal component compiles without TypeScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify dashboard, gallery, preview modal, and pricing page</name>
  <what-built>
    Dashboard page (invitation card grid + empty state + "+ New" card), template gallery (6 template cards + filter tabs + free limit banner), template preview modal (full-screen with desktop/mobile toggle + "Use this template" CTA), and pricing page (3 tier cards).
  </what-built>
  <how-to-verify>
    1. Start: `pnpm dev`
    2. Sign in with Clerk and land on `/dashboard`
    3. Verify empty state shows welcome message and "Create your first invitation" CTA
    4. Click "New invitation" → confirm you land on `/gallery`
    5. Verify gallery shows 6 template cards in a grid with thumbnails
    6. Click "Nunta" filter → verify 4 cards remain. Click "Botez" → verify 2 cards. Click "Toate" → verify 6 cards
    7. Click any template card → verify full-screen preview modal opens
    8. In modal: click mobile toggle → verify template shows at ~375px width with device frame
    9. Click desktop toggle → verify full-width preview
    10. Click "Use this template" → verify it navigates to `/editor/{new-id}` (editor not yet built — 404 is OK)
    11. Go back to `/dashboard` — verify the new invitation card appears with "Draft" badge
    12. Visit `/pricing` — verify 3 tier cards with "Coming soon" on Gold/Platinum CTAs
    13. Verify no emojis used as icons anywhere (all should be lucide-react SVG)
    14. Resize to 375px width — verify no horizontal overflow on any page
  </how-to-verify>
  <action>Human verification step. Follow the how-to-verify steps above.</action>
  <verify>
    <automated>MISSING — human-only verification (visual/functional)</automated>
  </verify>
  <done>User types "approved" confirming all 14 verification steps pass</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm build` exits 0
2. `pnpm test:unit` passes (existing tests unaffected)
3. Human checkpoint approved (see Task 3)
</verification>

<success_criteria>
- Dashboard renders card grid or empty state based on DB invitation count
- Gallery shows 6 templates with correct filter behavior (6/4/2)
- Preview modal opens with desktop/mobile toggle, renders real template component
- "Use this template" POST creates DB record and navigates to editor
- Free limit banner appears when user has 3+ drafts
- Pricing page shows 3 tier cards, Gold/Platinum CTAs show "Coming soon"
- Human checkpoint approved
- `pnpm build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-free-tier-mvp/01-04-SUMMARY.md`
</output>
