---
phase: 01-free-tier-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - middleware.ts
  - src/app/layout.tsx
  - src/app/(auth)/sign-in/[[...sign-in]]/page.tsx
  - src/app/(auth)/sign-up/[[...sign-up]]/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/components/nav/TopNav.tsx
  - src/lib/feature-gate.ts
  - src/i18n/ro.json
  - src/i18n/en.json
  - vitest.config.ts
  - playwright.config.ts
  - tests/unit/schema.test.ts
  - tests/unit/feature-gate.test.ts
  - tests/unit/deployment.test.ts
  - tests/unit/git-service.test.ts
  - tests/unit/autosave.test.ts
  - tests/e2e/gallery.spec.ts
  - tests/e2e/editor.spec.ts
  - tests/e2e/publish.spec.ts
  - .env.local.example
  - .env.example
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REQ-04
must_haves:
  truths:
    - "Running `pnpm build` exits 0 on the scaffolded project"
    - "Visiting /sign-in redirects unauthenticated users to Clerk hosted sign-in"
    - "Visiting /dashboard after auth renders the shell layout with top nav"
    - "RO/EN toggle is present in the top nav and persists selection to localStorage"
    - "All Wave 0 test stubs exist and `pnpm test:unit` completes without config errors"
  artifacts:
    - path: "middleware.ts"
      provides: "Clerk clerkMiddleware protecting all non-public routes"
    - path: "src/lib/feature-gate.ts"
      provides: "StubFeatureGate enforcing 3 draft / 1 published free tier limits"
    - path: "src/components/nav/TopNav.tsx"
      provides: "Top navigation: Logo | Tier badge | RO/EN toggle | Clerk account menu"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with jsdom + path aliases"
    - path: "playwright.config.ts"
      provides: "Playwright base URL http://localhost:3000"
  key_links:
    - from: "middleware.ts"
      to: "Clerk auth"
      via: "clerkMiddleware() from @clerk/nextjs/server"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/nav/TopNav.tsx"
      via: "import TopNav"
---

<objective>
Scaffold the complete Next.js 16 application: install all Phase 1 dependencies, configure Clerk auth middleware, wire the top nav shell, create Wave 0 test infrastructure stubs, and configure CI/CD.

Purpose: Every subsequent plan builds on this foundation. Auth, routing, and test harness must be in place before any feature work begins.
Output: Runnable Next.js 16 app with Clerk auth, top nav shell, i18n stub, and complete test infrastructure (stubs only — tests are red until their respective plans implement the features).
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-free-tier-mvp/01-CONTEXT.md
@.planning/phases/01-free-tier-mvp/01-RESEARCH.md
@.planning/phases/01-free-tier-mvp/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Next.js 16, install all dependencies, configure test infrastructure</name>
  <files>
    package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, tailwind.config.ts,
    postcss.config.mjs, vitest.config.ts, playwright.config.ts,
    tests/unit/schema.test.ts, tests/unit/feature-gate.test.ts,
    tests/unit/deployment.test.ts, tests/unit/git-service.test.ts,
    tests/unit/autosave.test.ts, tests/e2e/gallery.spec.ts,
    tests/e2e/editor.spec.ts, tests/e2e/publish.spec.ts,
    .env.local.example, .env.example
  </files>
  <behavior>
    - `pnpm test:unit` runs without config errors (all stubs are pending/skipped, not failing)
    - `pnpm build` exits 0 on a clean scaffold
    - `vitest.config.ts` resolves `@/` alias to `src/`
    - `playwright.config.ts` has baseURL `http://localhost:3000`
  </behavior>
  <action>
    1. Scaffold with pnpm:
       ```
       pnpm create next-app@latest . --typescript --tailwind --app --turbopack --no-git
       ```
       If the directory is non-empty, initialize manually: create `package.json` with the fields below.

    2. Install all Phase 1 production dependencies at the pinned versions from RESEARCH.md:
       ```
       pnpm add @clerk/nextjs@7.0.1 drizzle-orm@0.45.1 @neondatabase/serverless@1.0.2 @vercel/blob@2.3.1 @octokit/rest@22.0.1 resend@6.9.3 react-hook-form zod @hookform/resolvers @tanstack/react-query date-fns lucide-react next-intl
       ```

    3. Install dev dependencies:
       ```
       pnpm add -D drizzle-kit vitest @vitejs/plugin-react @testing-library/react @testing-library/user-event jsdom @playwright/test msw
       ```
       Then: `pnpm exec playwright install chromium`

    4. Configure `vitest.config.ts`:
       - environment: 'jsdom'
       - resolve alias `@` → `src/`
       - include: `tests/unit/**/*.test.{ts,tsx}`
       - setupFiles if needed for testing-library

    5. Configure `playwright.config.ts`:
       - baseURL: `http://localhost:3000`
       - testDir: `./tests/e2e`
       - use: { headless: true }

    6. Add `package.json` scripts:
       - `"test:unit": "vitest run --reporter=verbose"`
       - `"test:e2e": "playwright test"`
       - `"test": "vitest run && playwright test"`

    7. Create Wave 0 test stubs (empty describe blocks, not failing tests):
       - `tests/unit/schema.test.ts` — `describe('InvitationFieldsSchema', () => { it.todo('validates all 7 fields') })`
       - `tests/unit/feature-gate.test.ts` — `describe('StubFeatureGate', () => { it.todo('blocks 4th draft') })`
       - `tests/unit/deployment.test.ts` — `describe('DeploymentService', () => { it.todo('pollStatus returns READY') })`
       - `tests/unit/git-service.test.ts` — `describe('GitService', () => { it.todo('creates branch for new invitation') })`
       - `tests/unit/autosave.test.ts` — `describe('useAutosave', () => { it.todo('fires after 2s debounce') })`
       - `tests/e2e/gallery.spec.ts` — `test.todo('gallery shows 6 templates')`
       - `tests/e2e/editor.spec.ts` — `test.todo('editor updates live preview')`
       - `tests/e2e/publish.spec.ts` — `test.todo('publish flow returns live URL')`

    8. Create `.env.local.example` and `.env.example` listing all required env vars:
       ```
       NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
       CLERK_SECRET_KEY=
       DATABASE_URL=
       VERCEL_TOKEN=
       VERCEL_TEAM_ID=
       GITHUB_PAT=
       GITHUB_OWNER=acstoian
       GITHUB_REPO=weddingapp
       RESEND_API_KEY=
       RESEND_FROM_DOMAIN=
       BLOB_READ_WRITE_TOKEN=
       ```

    9. Configure `next.config.ts` — add `images.remotePatterns` for `*.vercel-storage.com` (Vercel Blob CDN).
  </action>
  <verify>
    <automated>pnpm build && pnpm test:unit</automated>
  </verify>
  <done>
    `pnpm build` exits 0. `pnpm test:unit` runs and shows all stubs as todo/skipped with no config errors. All dependency packages are in node_modules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clerk auth middleware, route groups, top nav, i18n stub, FeatureGate stub</name>
  <files>
    middleware.ts,
    src/app/layout.tsx,
    src/app/(auth)/sign-in/[[...sign-in]]/page.tsx,
    src/app/(auth)/sign-up/[[...sign-up]]/page.tsx,
    src/app/(dashboard)/layout.tsx,
    src/app/(dashboard)/dashboard/page.tsx,
    src/components/nav/TopNav.tsx,
    src/lib/feature-gate.ts,
    src/i18n/ro.json,
    src/i18n/en.json,
    .github/workflows/ci.yml
  </files>
  <action>
    **UI NOTE:** Before writing any component code, run the ui-ux-pro-max design system script:
    ```
    python3 .claude/skills/ui-ux-pro-max/scripts/search.py "wedding invitation saas elegant minimal" --design-system --persist -p "Save the Date"
    ```
    Use the returned palette, typography, and spacing guidelines for ALL components in this task.

    1. `middleware.ts` — implement Clerk clerkMiddleware exactly per RESEARCH.md Pattern 10:
       - Public routes: `/`, `/sign-in(.*)`, `/sign-up(.*)`, `/pricing`, `/invitations/(.*)`
       - All other routes protected via `auth.protect()`
       - Export the `config.matcher` exactly as shown in Pattern 10

    2. `src/app/layout.tsx` — wrap with `<ClerkProvider>`. Set `lang="ro"` default. Import Google Fonts needed by Phase 1 templates (defer to later plan for specific fonts; for now import a generic Inter for the shell).

    3. Auth pages:
       - `src/app/(auth)/sign-in/[[...sign-in]]/page.tsx` — render `<SignIn />` from `@clerk/nextjs`
       - `src/app/(auth)/sign-up/[[...sign-up]]/page.tsx` — render `<SignUp />` from `@clerk/nextjs`
       - Center these on page with minimal padding; no custom styling needed (Clerk renders its own UI)

    4. `src/components/nav/TopNav.tsx` — Top nav bar per CONTEXT.md spec:
       - Left: Logo text "Save the Date" (link to /dashboard)
       - Center-right: Tier badge component (hardcoded "Free" badge in Phase 1; Phase 2 replaces with real tier)
       - Language toggle: "RO" | "EN" buttons. On click, write choice to localStorage key `lang` and reload with locale cookie set. Highlight active language.
       - Right: `<UserButton />` from `@clerk/nextjs` for account menu
       - Use lucide-react icons, NOT emojis. All clickable elements: `cursor-pointer`
       - Accessible: aria-labels on icon buttons, focus rings visible

    5. `src/app/(dashboard)/layout.tsx` — renders `<TopNav />` + `{children}`. Add padding-top to account for fixed nav height.

    6. `src/app/(dashboard)/dashboard/page.tsx` — placeholder page (empty card grid shell). Will be fully implemented in 01-04. For now: renders "Dashboard" heading inside the layout so routing is confirmed.

    7. `src/lib/feature-gate.ts` — implement StubFeatureGate exactly per RESEARCH.md Pattern 11:
       - `FeatureGate` interface with `canPublish`, `canCreateDraft`, `getUserTier`
       - `StubFeatureGate` class enforces FREE tier: 3 draft max, 1 published max
       - Export both interface and class
       - Import `db` from `lib/db/index.ts` (will be created in 01-02; use a type-only import stub so TypeScript doesn't fail now)
       - Export a singleton: `export const featureGate = new StubFeatureGate()`

    8. i18n stubs:
       - `src/i18n/ro.json` — `{ "nav": { "newInvitation": "Invitatie noua", "pricing": "Preturi" }, "dashboard": { "title": "Invitatiile mele", "empty": "Creeaza prima ta invitatie" } }`
       - `src/i18n/en.json` — same keys in English: `{ "nav": { "newInvitation": "New invitation", "pricing": "Pricing" }, "dashboard": { "title": "My Invitations", "empty": "Create your first invitation" } }`
       - Note: full next-intl wiring deferred to 01-04/01-05; these are the string files used once wired

    9. `.github/workflows/ci.yml` — GitHub Actions CI:
       ```yaml
       name: CI
       on: [push, pull_request]
       jobs:
         test:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - uses: actions/setup-node@v4
               with: { node-version: 20, cache: pnpm }
             - run: pnpm install --frozen-lockfile
             - run: pnpm build
             - run: pnpm test:unit
       ```
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    `pnpm build` exits 0. Visiting `/dashboard` while signed out redirects to Clerk sign-in. Visiting `/dashboard` while signed in renders the shell layout with top nav. RO/EN toggle is visible. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
Run in sequence:
1. `pnpm build` — must exit 0
2. `pnpm test:unit` — must run without config errors (stubs show as todo, not failing)
3. Start dev server, visit /sign-in — Clerk sign-in component renders
4. Visit /dashboard unauthenticated — redirected to /sign-in
5. Sign in, visit /dashboard — TopNav renders with Logo, Free badge, RO/EN toggle, Clerk UserButton
</verification>

<success_criteria>
- `pnpm build` exits 0 with no TypeScript errors
- `pnpm test:unit` runs without configuration errors; all stubs are todo/pending
- Clerk middleware protects /dashboard (redirects to sign-in when unauthenticated)
- TopNav renders all 4 elements: Logo, Tier badge, Language toggle, Clerk UserButton
- All Wave 0 test stub files exist at specified paths
- `.env.local.example` lists all 11 required environment variables
- CI workflow file exists at `.github/workflows/ci.yml`
</success_criteria>

<output>
After completion, create `.planning/phases/01-free-tier-mvp/01-01-SUMMARY.md`
</output>
